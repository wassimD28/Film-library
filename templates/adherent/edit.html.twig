{% extends 'base.html.twig' %}

{% block title %}Edit Adherent{% endblock %}

{% block body %}
    <div class="w-4/5 m-auto text-admin-text min-h-screen p-6">
        <h1 class="text-4xl font-bold mb-8">Edit Adherent</h1>

        {{ include('adherent/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('app_adherent_index') }}" class="inline-block mb-4 text-admin-text bg-admin-bg hover:bg-admin-text hover:text-admin-bg border border-admin-text px-4 py-2 rounded-lg mt-2 ease-in-out duration-200">Back to List</a>

        {{ include('adherent/_delete_form.html.twig') }}
    </div>
{% endblock %}

