{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
<form method="post" class="w-4/5 m-auto text-myColor-text min-h-screen p-6">
    {% if error %}
        <div class="bg-myColor-secondary text-myColor-text p-4 rounded mb-4">
            {{ error.messageKey|trans(error.messageData, 'security') }}
        </div>
    {% endif %}

    {% if app.user %}
        <div class="mb-3 text-myColor-text">
            You are logged in as {{ app.user.email }}, <a href="{{ path('app_logout') }}" class="text-myColor-text underline">Logout</a>
        </div>
    {% endif %}

    {# <div class="py-12 border-b border-myColor-accent">
        <h1 class="text-6xl pt-12 pb-8 font-bold">
            Login
        </h1>

        <div class="mb-4">
            <label for="inputEmail" class="block text-myColor-text text-sm font-bold mb-2">Email</label>
            <input 
                type="email" 
                value="{{ last_username }}" 
                name="email" 
                id="inputEmail" 
                class="w-full px-3 py-2 border rounded-lg bg-myColor-semibg2 text-white border-gray-700" 
                placeholder="Email"
                autocomplete="email" 
                required 
                autofocus
            >
        </div>

        <div class="mb-4">
            <label for="inputPassword" class="block text-myColor-text text-sm font-bold mb-2">Password</label>
            <input 
                type="password" 
                name="password" 
                id="inputPassword" 
                class="w-full px-3 py-2 border rounded-lg bg-myColor-semibg2 text-myColor-text border-gray-700" 
                autocomplete="current-password" 
                placeholder="Password"
                required
            >
        </div>

        <input 
            type="hidden" 
            name="_csrf_token"
            value="{{ csrf_token('authenticate') }}"
        >

        <button 
            type="submit" 
            class="bg-purple-700 hover:bg-purple-800 text-myColor-text font-medium py-2 px-4 rounded-lg">
            Sign in
        </button>
        <a href="{{ path('app_register') }}"  class="bg-pink-700 hover:bg-pink-800 text-myColor-text font-medium py-2 px-4 rounded-lg">
            Register
        </a>
    </div> #}

    <!-- TEST -->
   <section class="bg-myColor-bg">
        <div class="flex flex-col items-center justify-center px-6 py-8 mx-auto md:h-screen lg:py-0">
            <a href="#" class="flex items-center mb-6 text-2xl font-semibold text-myColor-text">
                <img class="h-8 mr-2" src="{{asset('shared/images/watchTheMovie.png')}}" alt="logo">
                Whatch the movie    
            </a>
            <div class="w-full bg-myColor-semibg rounded-lg shadow md:mt-0 sm:max-w-md xl:p-0">
                <div class="p-6 space-y-4 md:space-y-6 sm:p-8">
                    <h1 class="text-xl font-bold leading-tight tracking-tight text-myColor-primary md:text-2xl">
                        Sign in to your account
                    </h1>
                    <form class="space-y-4 md:space-y-6" action="#">
                        <div>
                            <label for="inputEmail" class="block mb-2 text-sm font-medium text-myColor-accent dark:text-white">Email</label>
                            <input 
                                type="email" 
                                value="{{ last_username }}" 
                                name="email" 
                                id="inputEmail" 
                                class="bg-myColor-semibg2 border border-myColor-text text-myColor-text sm:text-sm rounded-lg focus:ring-myColor-accent focus:border-myColor-accent block w-full p-2.5"
                                placeholder="Email" 
                                autocomplete="email" 
                                required 
                                autofocus
                            >
                        </div>
                        <div>
                            <label for="inputPassword" class="block mb-2 text-sm font-medium text-myColor-accent dark:text-white">Password</label>
                            <input 
                                type="password" 
                                name="password" 
                                id="inputPassword" 
                                placeholder="Password" 
                                class="bg-myColor-semibg2 border border-myColor-text text-myColor-text sm:text-sm rounded-lg focus:ring-myColor-accent focus:border-myColor-accent block w-full p-2.5" 
                                required
                            >
                        </div>
                        <input 
                            type="hidden" 
                            name="_csrf_token"
                            value="{{ csrf_token('authenticate') }}"
                        >
                        <button 
                            type="submit" 
                            class="w-full text-myColor-text bg-myColor-secondary hover:bg-myColor-accent focus:ring-4 focus:outline-none focus:ring-primary-300 hover:text-myColor-semibg2 ease-in-out duration-200 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-primary-600 dark:hover:bg-primary-700 dark:focus:ring-primary-800">
                            Sign in
                        </button>
                        <p class="text-sm font-light text-gray-500 dark:text-gray-400">
                            Don’t have an account yet? 
                            <a href="{{ path('app_register') }}" class="font-medium text-myColor-accent hover:underline dark:text-primary-500">
                                Register
                            </a>
                        </p>
                    </form>
                </div>
            </div>
        </div>
    </section>

</form>
{% endblock %}
