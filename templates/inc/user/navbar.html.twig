<nav aria-label="menu nav" class="bg-myColor-semibg pt-2 md:pt-1 pb-1 px-1 mt-0 h-auto fixed w-full z-20 top-0">

    <div class="flex flex-wrap items-center">
        <div class="flex flex-shrink md:w-1/3 justify-center md:justify-start text-white">
            <a href="#" aria-label="Home">
                <span class="text-2xl pl-2 flex flex-row items-center font-bold uppercase text-red-500"><img class=" h-[40px]" src="{{ asset('shared/images/watchTheMovie.png') }}" alt=""></span>
            </a>
        </div>

        <div class="flex flex-1 md:w-1/3 justify-center md:justify-start text-myColor-text px-2">
            <span class="relative w-full">
                <form  method="get" action="{{ path('app_findFilms_index') }}">
                    <input type="text" name="query" value="{{ query|default('') }}" placeholder="Search for a film..." class="w-full bg-myColor-semibg2 text-myColor-text transition focus:outline-none border border-myColor-semibg focus:border-myColor-accent rounded py-3 px-2 pl-10 appearance-none leading-normal">
                    <button type="submit" class="absolute search-icon cursor-pointer left-3 top-3">
                        <i class="text-myColor-text text-lg fa-solid fa-magnifying-glass"></i>
                    </button>
                </form>
            </span>
        </div>

        <div class="flex w-full pt-2 content-center justify-between md:w-1/3 md:justify-end">
            <ul class="list-reset flex justify-between flex-1 md:flex-none items-center">
                <li class="flex-1 md:flex-none md:mr-3">
                    {% if app.user %}
                        <div class="inline-block text-myColor-bg bg-myColor-accent rounded-md rounded-tr-none rounded-br-none ease-in-out duration-200 py-2 px-2 no-underline">{{ user_service.getUser().email }}</div>
                        <a class="inline-block border border-myColor-accent text-myColor-accent hover:text-myColor-bg hover:bg-myColor-accent rounded-tl-none rounded-bl-none px-2 rounded-md ease-in-out duration-200 py-2  no-underline" href="/logout">Log out</a>
                    {% else %}
                        <a class="inline-block py-2 px-4 text-white no-underline" href="/logout">Log out</a>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>

</nav>